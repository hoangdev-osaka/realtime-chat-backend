spring:
  docker:
    compose:
      enabled: false
  datasource:
    url: jdbc:mysql://localhost:3306/realtime_chat
    username: root
    password:
logging:
  level:
    org.hibernate.orm.jdbc.bind: TRACE
